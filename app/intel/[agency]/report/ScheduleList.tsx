'use client';

// Replaced with the full API-backed list in Task 16. For now this stub
// keeps the page render path clean while the schedules CRUD API ships.
export function ScheduleList({ agency: _agency }: { agency: string }) {
  return null;
}
